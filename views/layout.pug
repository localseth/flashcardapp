doctype html
html(lang="en")
    head
        title Flash Cards
    body
        include includes/header.pug
        section#content 
        if name
            h2 Welcome, #{name}!
            form(action='/goodbye' method='post')
                button goodbye
        block content
        include includes/footer.pug